<!DOCTYPE html>
<html lang="en">

<%- include('partials/head', { pageTitle: 'TasteLoop - Discover Anime Openings & Endings' }) %>

<body class="min-h-screen bg-gradient-to-b from-zinc-950 via-zinc-900 to-black text-white">
  <%- include('partials/header') %>

  <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-10">
    <% if (typeof q !== 'undefined' && q) { %>
      <div class="mb-6 text-sm text-white/80">
        <span>Results for</span>
        <span class="px-2 py-0.5 rounded bg-white/5 border border-white/10 text-white">"<%= q %>"</span>
        <span class="ml-2">(<%= dataset.length %>)</span>
        <a href="/" class="ml-3 text-yellow-400 hover:text-yellow-300">Clear</a>
      </div>

      <section class="mb-10">
        <div class="mb-4">
          <h1 class="text-3xl sm:text-4xl font-semibold tracking-tight">Search results</h1>
          <p class="text-white/70">Matching titles from the dataset</p>
        </div>
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-5">
          <% dataset.forEach((anime) => { %>
            <article class="group relative rounded-xl overflow-hidden bg-white/5 border border-white/10 shadow-sm hover:shadow-md transition">
              <div class="aspect-video bg-black/40 overflow-hidden">
                <img class="w-full h-full object-cover transition group-hover:scale-[1.03]" src="<%= anime.images.jpg.large_image_url %>" alt="<%= anime.title %> poster">
              </div>
              <div class="p-3">
                <h3 class="text-base font-semibold truncate"><%= anime.title %></h3>
                <p class="text-sm text-white/70 truncate"><%= anime.title_japanese || '' %></p>
                <div class="mt-3 flex items-center justify-between text-xs text-white/70">
                  <div class="flex items-center gap-3">
                    <span><%= anime.year ?? '-' %></span>
                    <span>⭐ <%= anime.score?.toFixed?.(1) ?? anime.score %></span>
                  </div>
                  <a href="/anime/<%= anime.mal_id %>" class="inline-flex items-center gap-1 px-2 py-1 rounded-md bg-yellow-500 text-black font-medium hover:bg-yellow-400">Details</a>
                </div>
              </div>
            </article>
          <% }) %>
        </div>
      </section>
    <% } else { %>
      <section class="relative isolate overflow-hidden rounded-2xl border border-white/10 bg-white/5 mb-12">
        <div aria-hidden="true" class="pointer-events-none absolute inset-0">
          <div class="absolute inset-0 bg-[radial-gradient(ellipse_at_top,rgba(255,255,255,0.06),transparent_55%)]"></div>
          <div class="absolute -top-20 right-[-10%] h-[28rem] w-[28rem] rounded-full blur-3xl opacity-20 bg-[conic-gradient(at_30%_30%,#f59e0b,transparent_35%_65%,#f59e0b)]"></div>
          <div class="absolute -bottom-16 left-[-10%] h-[22rem] w-[22rem] rounded-full blur-3xl opacity-10 bg-[conic-gradient(at_70%_70%,#fde68a,transparent_30%_70%,#fde68a)]"></div>
        </div>

        <div class="relative px-6 py-14 sm:px-12 lg:px-16">
          <div class="max-w-3xl">
            <h1 class="text-4xl sm:text-5xl font-extrabold tracking-tight">
              <span class="bg-gradient-to-b from-yellow-300 via-yellow-400 to-yellow-600 bg-clip-text text-transparent">Loop into the best anime intros</span>
            </h1>
            <p class="mt-3 text-white/70 text-base sm:text-lg leading-relaxed">
              TasteLoop helps you discover openings and endings from your favorite series - fast, clean, and to the point.
            </p>

            <form action="/" method="get" class="mt-6 flex items-stretch gap-2">
              <label for="q" class="sr-only">Search titles, JP names, or genres</label>
              <input id="q" name="q" type="search" placeholder="Search titles, JP names, or genres..."
                     class="w-full px-4 py-3 rounded-lg bg-black/30 border border-white/10 text-white placeholder-white/50 focus:outline-none focus:ring-2 focus:ring-yellow-500/40" />
              <button type="submit" class="px-5 py-3 rounded-lg bg-yellow-500 text-black font-semibold hover:bg-yellow-400">Search</button>
            </form>

            <% 
              const totalAnime = dataset.length;
              const totalOpenings = dataset.reduce((s,a)=>s+(a.themes?.openings?.length||0),0);
              const uniqArtists = (()=>{ const set = new Set(); dataset.forEach(a=> (a.themes?.openings||[]).forEach(t=>{ const m = /by\s+([^()]+)\s*/i.exec(t); if(m) set.add(m[1].trim()); })); return set.size; })();
            %>
            <div class="mt-5 flex flex-wrap gap-2 text-xs text-white/70">
              <span class="px-2 py-1 rounded-md border border-white/10 bg-white/5"><%= totalAnime.toLocaleString('en-US') %> titles</span>
              <span class="px-2 py-1 rounded-md border border-white/10 bg-white/5"><%= totalOpenings.toLocaleString('en-US') %> openings</span>
              <span class="px-2 py-1 rounded-md border border-white/10 bg-white/5"><%= uniqArtists.toLocaleString('en-US') %> artists</span>
            </div>
          </div>
        </div>
      </section>

      <section class="mb-12">
        <div class="mb-4 flex items-end justify-between">
          <div>
            <h2 class="text-2xl sm:text-3xl font-semibold tracking-tight">Trending now</h2>
            <p class="text-white/70">Top TV anime by popularity</p>
          </div>
        </div>
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-5">
          <% dataset.slice().sort((a,b)=>a.popularity-b.popularity).slice(0,8).forEach((anime, i) => { %>
            <article class="group relative rounded-xl overflow-hidden bg-white/5 border border-white/10">
              <div class="aspect-video bg-black/40 overflow-hidden">
                <img class="w-full h-full object-cover transition group-hover:scale-[1.03]" src="<%= anime.images.jpg.large_image_url %>" alt="<%= anime.title %> poster">
              </div>
              <div class="p-3">
                <div class="flex items-center justify-between mb-1">
                  <h3 class="text-base font-semibold truncate max-w-[75%]"><%= anime.title %></h3>
                  <span class="text-xs px-1.5 py-0.5 rounded bg-yellow-500/10 text-yellow-400 border border-yellow-500/20">#<%= i+1 %></span>
                </div>
                <p class="text-sm text-white/70 truncate"><%= anime.title_japanese || '' %></p>
                <div class="mt-3 flex items-center justify-between text-xs text-white/70">
                  <span><%= anime.year ?? '-' %></span>
                  <a href="/anime/<%= anime.mal_id %>" class="inline-flex items-center gap-1 px-2 py-1 rounded-md bg-yellow-500 text-black font-medium hover:bg-yellow-400">Details</a>
                </div>
              </div>
            </article>
          <% }) %>
        </div>
      </section>

      <section>
        <div class="mb-4">
          <h2 class="text-2xl sm:text-3xl font-semibold tracking-tight">Highest rated</h2>
          <p class="text-white/70">Fan favorites by score</p>
        </div>
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-5">
          <% dataset.slice().sort((a,b)=>b.score-a.score).slice(0,8).forEach((anime) => { %>
            <article class="group relative rounded-xl overflow-hidden bg-white/5 border border-white/10">
              <div class="aspect-video bg-black/40 overflow-hidden">
                <img class="w-full h-full object-cover transition group-hover:scale-[1.03]" src="<%= anime.images.jpg.large_image_url %>" alt="<%= anime.title %> poster">
              </div>
              <div class="p-3">
                <div class="flex items-center justify-between mb-1">
                  <h3 class="text-base font-semibold truncate max-w-[75%]"><%= anime.title %></h3>
                  <span class="text-xs px-1.5 py-0.5 rounded bg-emerald-500/10 text-emerald-400 border border-emerald-500/20">⭐ <%= anime.score?.toFixed?.(1) ?? anime.score %></span>
                </div>
                <p class="text-sm text-white/70 truncate"><%= anime.title_japanese || '' %></p>
                <div class="mt-3 flex items-center justify-between text-xs text-white/70">
                  <span><%= anime.year ?? '-' %></span>
                  <a href="/anime/<%= anime.mal_id %>" class="inline-flex items-center gap-1 px-2 py-1 rounded-md bg-yellow-500 text-black font-medium hover:bg-yellow-400">Details</a>
                </div>
              </div>
            </article>
          <% }) %>
        </div>
      </section>
    <% } %>
  </main>

  <%- include('partials/footer') %>
</body>

</html>
